// Function that posts your channel based on json stored info
module.exports.post = (user) => {
}

// Catches any twitch posts outside of stream announcements, deletes them and puts them in stream annoucements
module.exports.switch = (twitch) => {
}

// lists available and most recent streams
module.export.streams
